export const COACH_MARKDOWN_FORMATTING_RULES = `OUTPUT FORMAT
- Use concise Markdown. Prefer short paragraphs with one idea each.
- Wrap identifiers, function names, symbols, short expressions, file names, and test names in backticks.
- If you show multi-line code, use a fenced code block with an accurate language tag, such as \`\`\`python, \`\`\`typescript, \`\`\`go, or \`\`\`scala.
- Prefer inline code for a single condition or expression instead of a fenced block.
- Use Markdown bullets or tables for structured comparisons when they improve readability. For tables, include the header separator row so the renderer can format them.
- Use standard LaTeX delimiters for math: \`$...$\` inline and \`$$...$$\` for display math.`;
