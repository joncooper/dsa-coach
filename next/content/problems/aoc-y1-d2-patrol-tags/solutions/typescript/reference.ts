export function count_valid_tags(inputText: string): number { let valid = 0; for (const raw of inputText.split("\n")) { const line = raw.trim(); if (!line) continue; const space = line.indexOf(" "); const bounds = line.slice(0, space); const rest = line.slice(space + 1); const [low, high] = bounds.split("-").map(Number); const [charPart, word] = rest.split(": "); const count = [...word].filter((ch) => ch === charPart[0]).length; if (low <= count && count <= high) valid += 1; } return valid; }
