export function count_valid_positions(inputText: string): number { let valid = 0; for (const raw of inputText.split("\n")) { const line = raw.trim(); if (!line) continue; const space = line.indexOf(" "); const bounds = line.slice(0, space); const rest = line.slice(space + 1); const [a, b] = bounds.split("-").map((value) => Number(value) - 1); const [charPart, word] = rest.split(": "); const first = a >= 0 && a < word.length && word[a] === charPart[0]; const second = b >= 0 && b < word.length && word[b] === charPart[0]; if (first !== second) valid += 1; } return valid; }
