export function count_valid_permits(inputText: string): number { let valid = 0; for (const raw of inputText.split("\n")) { const parts = raw.trim().split(" "); if (parts.length !== 3) continue; if (isValidPermit(parts[1], parts[2])) valid += 1; } return valid; } function isValidPermit(licenseValue: string, expires: string): boolean { const groups = licenseValue.split("-"); return groups.length === 3 && groups[0].length === 3 && groups[1].length === 2 && groups[2].length === 4 && groups.every((group) => /^\d+$/.test(group)) && /^\d{4}$/.test(expires) && Number(expires) >= 2025; }
