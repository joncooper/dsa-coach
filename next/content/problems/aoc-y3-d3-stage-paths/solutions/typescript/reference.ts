export function diagonal_count(inputText: string): number { const rows = inputText.split("\n").filter(Boolean); if (!rows.length) return 0; let r = 0, c = 0, count = 0; while (r < rows.length && c < rows[0].length) { const cell = rows[r][c]; if (cell === "#") break; if (cell === "T") count += 1; r += 1; c += 1; } return count; }
