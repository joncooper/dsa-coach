export function three_paths_sum(inputText: string): number { const rows = inputText.split("\n").filter(Boolean); if (!rows.length) return 0; let total = 0; for (const [dr, dc] of [[1,1],[1,2],[2,1]] as const) { let r = 0, c = 0; while (r < rows.length && c < rows[0].length) { const cell = rows[r][c]; if (cell === "#") break; if (cell === "T") total += 1; r += dr; c += dc; } } return total; }
