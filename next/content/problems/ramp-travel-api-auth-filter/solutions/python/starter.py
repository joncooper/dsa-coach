import requests


def fetch_engineers(base_url: str, token: str) -> list[dict]:
    """Return every employee whose department is 'engineering'."""
    # TODO: send the Bearer token, filter on the server, and paginate
    raise NotImplementedError
