export function count_complete_records(inputText: string): number { const required = new Set(["id","name","age","grade","cohort"]); let count = 0; for (const block of inputText.split("\n\n")) { if (!block.trim()) continue; const keys = new Set<string>(); for (const token of block.split(/\s+/)) if (token.includes(":")) keys.add(token.split(":", 1)[0]); if ([...required].every((key) => keys.has(key))) count += 1; } return count; }
