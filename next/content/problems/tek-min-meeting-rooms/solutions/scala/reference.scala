object Solution { def min_meeting_rooms(meetings: Seq[Seq[Int]]): Int = { val events = meetings.flatMap(m => Seq((m(0), 1), (m(1), -1))).sortBy(event => (event._1, event._2)); var active = 0; var peak = 0; for ((_, delta) <- events) { active += delta; peak = math.max(peak, active) }; peak } }
