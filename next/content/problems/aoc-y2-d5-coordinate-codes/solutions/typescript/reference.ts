export function max_manhattan(inputText: string): number { let best = 0; for (const raw of inputText.split("\n")) { const line = raw.trim(); if (!line) continue; let row = 0, col = 0; for (let i = 0; i < line.length; i += 2) { const [dr, dc] = step(line.slice(i, i + 2)); row += dr; col += dc; } best = Math.max(best, Math.abs(row) + Math.abs(col)); } return best; } function step(pair: string): [number, number] { if (pair === "NN") return [1,0]; if (pair === "SS") return [-1,0]; if (pair === "EE") return [0,1]; return [0,-1]; }
