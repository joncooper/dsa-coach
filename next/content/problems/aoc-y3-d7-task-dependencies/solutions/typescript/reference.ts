export function longest_chain(inputText: string): number { const { children, nodes } = parseTasks(inputText); if (!nodes.size) return 0; const memo = new Map<string, number>(); const depth = (node: string): number => { if (memo.has(node)) return memo.get(node)!; const kids = children.get(node) ?? []; const result = kids.length === 0 ? 1 : 1 + Math.max(...kids.map(depth)); memo.set(node, result); return result; }; return Math.max(...[...nodes].map(depth)); } function parseTasks(inputText: string): { children: Map<string, string[]>; nodes: Set<string> } { const children = new Map<string, string[]>(); const nodes = new Set<string>(); for (const raw of inputText.split("\n")) { const line = raw.trim(); if (!line.endsWith(".") || !line.includes(" before ")) continue; const [headRaw, tailRaw] = line.slice(0, -1).split(" before ", 2); const head = headRaw.trim(); nodes.add(head); if (tailRaw.trim() === "nothing") { children.set(head, children.get(head) ?? []); continue; } const kids = tailRaw.split(",").map((token) => token.trim()).filter(Boolean); children.set(head, [...(children.get(head) ?? []), ...kids]); for (const kid of kids) { nodes.add(kid); if (!children.has(kid)) children.set(kid, []); } } return { children, nodes }; }
