export function find_missing_seat(inputText: string): number { const ids = new Set<number>(); for (const raw of inputText.split("\n")) { const line = raw.trim(); if (!line) continue; ids.add(parseInt(line.replace(/[FL]/g, "0").replace(/[BR]/g, "1"), 2)); } if (ids.size === 0) return -1; const sorted = [...ids].sort((a, b) => a - b); for (let i = 0; i < sorted.length - 1; i += 1) if (sorted[i + 1] - sorted[i] === 2) return sorted[i] + 1; return -1; }
