export function max_seat_id(inputText: string): number { let best = -1; for (const raw of inputText.split("\n")) { const line = raw.trim(); if (!line) continue; const bits = line.replace(/[FL]/g, "0").replace(/[BR]/g, "1"); best = Math.max(best, parseInt(bits, 2)); } return best; }
