export function max_revenue_window(inputText: string): number { const lines = inputText.split("\n").filter((line) => line.trim()); if (!lines.length) return 0; const k = Number(lines[0]); const days = lines.slice(1).map(Number); if (days.length < k) return 0; let current = days.slice(0, k).reduce((a, b) => a + b, 0); let best = current; for (let i = k; i < days.length; i += 1) { current += days[i] - days[i - k]; best = Math.max(best, current); } return best; }
