export function rising_windows(inputText: string): number { const lines = inputText.split("\n").filter((line) => line.trim()); if (!lines.length) return 0; const k = Number(lines[0]); const days = lines.slice(1).map(Number); if (days.length <= k) return 0; let prev = days.slice(0, k).reduce((a, b) => a + b, 0); let rising = 0; for (let i = k; i < days.length; i += 1) { const next = prev + days[i] - days[i - k]; if (next > prev) rising += 1; prev = next; } return rising; }
