export function majority_tag_count(inputText: string): number { let total = 0; for (const block of inputText.split("\n\n")) { const counts = new Map<string, number>(); let lines = 0; for (const raw of block.split("\n")) { const line = raw.trim(); if (!line) continue; lines += 1; for (const tag of new Set(line.split(",").filter(Boolean))) counts.set(tag, (counts.get(tag) ?? 0) + 1); } if (lines > 0) { const threshold = Math.floor((lines + 1) / 2); for (const count of counts.values()) if (count >= threshold) total += 1; } } return total; }
