export function odd_tag_count(inputText: string): number { let total = 0; for (const block of inputText.split("\n\n")) { const counts = new Map<string, number>(); let hadLine = false; for (const raw of block.split("\n")) { const line = raw.trim(); if (!line) continue; hadLine = true; for (const tag of new Set(line.split(",").filter(Boolean))) counts.set(tag, (counts.get(tag) ?? 0) + 1); } if (hadLine) for (const count of counts.values()) if (count % 2 === 1) total += 1; } return total; }
