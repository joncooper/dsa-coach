export function shelf_overlap(inputText: string): number { let total = 0; for (const block of inputText.split("\n\n")) { const shelves = block.split("\n").map((line) => line.trim()).filter(Boolean).map(parseSet); if (shelves.length === 0) continue; const common = new Set(shelves[0]); for (const shelf of shelves.slice(1)) for (const id of [...common]) if (!shelf.has(id)) common.delete(id); total += common.size; } return total; } function parseSet(line: string): Set<number> { return new Set(line.split(",").map(Number)); }
