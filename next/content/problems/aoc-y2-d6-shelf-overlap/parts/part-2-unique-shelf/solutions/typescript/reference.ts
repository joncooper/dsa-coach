export function shelf_unique(inputText: string): number { let total = 0; for (const block of inputText.split("\n\n")) { const counts = new Map<number, number>(); let any = false; for (const raw of block.split("\n")) { const line = raw.trim(); if (!line) continue; any = true; for (const id of new Set(line.split(",").map(Number))) counts.set(id, (counts.get(id) ?? 0) + 1); } if (any) for (const count of counts.values()) if (count === 1) total += 1; } return total; }
