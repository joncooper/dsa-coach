export function valid_cipher_count(inputText: string): number { let valid = 0; for (const raw of inputText.split("\n")) { const parts = raw.trim().split(" "); if (parts.length !== 3) continue; const [, code, status] = parts; if (status === "ok" && /[A-Za-z]/.test(code) && /\d/.test(code)) valid += 1; } return valid; }
