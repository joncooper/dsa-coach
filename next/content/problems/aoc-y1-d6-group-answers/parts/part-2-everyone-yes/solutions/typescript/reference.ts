export function everyone_yes_sum(inputText: string): number { let total = 0; for (const block of inputText.split("\n\n")) { const persons = block.split("\n").map((line) => line.trim()).filter(Boolean); if (persons.length === 0) continue; const common = new Set(persons[0]); for (const person of persons.slice(1)) for (const ch of [...common]) if (!person.includes(ch)) common.delete(ch); total += common.size; } return total; }
