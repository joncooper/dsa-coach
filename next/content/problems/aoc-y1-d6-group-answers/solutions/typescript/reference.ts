export function anyone_yes_sum(inputText: string): number { let total = 0; for (const block of inputText.split("\n\n")) { const union = new Set<string>(); for (const line of block.split("\n")) for (const ch of line.trim()) union.add(ch); total += union.size; } return total; }
