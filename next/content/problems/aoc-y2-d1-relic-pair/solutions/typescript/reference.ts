export function balanced_pair_count(inputText: string): number { const counts = valueCounts(inputText); let pairs = 0; for (const count of counts.values()) pairs += Math.floor(count * (count - 1) / 2); return pairs; } function valueCounts(inputText: string): Map<number, number> { const counts = new Map<number, number>(); for (const raw of inputText.split("\n")) { const line = raw.trim(); if (!line) continue; const value = Number(line); counts.set(value, (counts.get(value) ?? 0) + 1); } return counts; }
