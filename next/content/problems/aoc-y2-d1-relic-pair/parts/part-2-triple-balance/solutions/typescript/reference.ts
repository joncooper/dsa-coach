export function balanced_triple_count(inputText: string): number { const counts = new Map<number, number>(); for (const raw of inputText.split("\n")) { const line = raw.trim(); if (!line) continue; const value = Number(line); counts.set(value, (counts.get(value) ?? 0) + 1); } let triples = 0; for (const count of counts.values()) if (count >= 3) triples += Math.floor(count * (count - 1) * (count - 2) / 6); return triples; }
