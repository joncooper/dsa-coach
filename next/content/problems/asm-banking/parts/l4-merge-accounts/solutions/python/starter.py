def solution(queries):
    balances = {}
    spent = {}
    payments = {}
    results = []
    for query in queries:
        op = query[0]
        # Level 1-3 ops, plus:
        # MERGE_ACCOUNTS ts primary secondary -> "<new balance>" / ""
        results.append("")
    return results
