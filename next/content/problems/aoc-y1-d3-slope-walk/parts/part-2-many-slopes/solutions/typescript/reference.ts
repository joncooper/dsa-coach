export function slope_walk_product(inputText: string): number { const rows = inputText.split("\n").filter(Boolean); if (rows.length === 0) return 0; const width = rows[0].length; let product = 1; for (const [dx, dy] of [[1,1],[3,1],[5,1],[7,1],[1,2]] as const) { let trees = 0, r = 0, c = 0; while (r < rows.length) { if (rows[r][c % width] === "#") trees += 1; r += dy; c += dx; } product *= trees; } return product; }
