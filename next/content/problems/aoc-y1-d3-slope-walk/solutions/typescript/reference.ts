export function slope_walk(inputText: string): number { const rows = inputText.split("\n").filter(Boolean); if (rows.length === 0) return 0; const width = rows[0].length; let trees = 0; for (let r = 0; r < rows.length; r += 1) if (rows[r][(r * 3) % width] === "#") trees += 1; return trees; }
