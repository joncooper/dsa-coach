object Solution { def resolve_path(path: String): String = { val stack = scala.collection.mutable.ArrayBuffer[String](); for (part <- path.split("/")) { if (part == "" || part == ".") () else if (part == "..") { if (stack.nonEmpty) stack.remove(stack.length - 1) } else stack += part }; "/" + stack.mkString("/") } }
