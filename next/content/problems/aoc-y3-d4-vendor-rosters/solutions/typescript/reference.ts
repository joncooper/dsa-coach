export function valid_roster_total(inputText: string): number { const required = new Set(["food","craft","music"]); let total = 0; for (const block of inputText.split("\n\n")) { const categories = new Set<string>(); let itemCount = 0; for (const raw of block.split("\n")) { const line = raw.trim(); if (!line.includes(":")) continue; const [category, items] = line.split(":", 2); categories.add(category); itemCount += items.split(",").length; } if ([...required].every((cat) => categories.has(cat))) total += itemCount; } return total; }
