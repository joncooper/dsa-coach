export function scan_aisle(inputText: string): number { let relics = 0; for (const row of inputText.split("\n").filter(Boolean)) { for (const ch of row) { if (ch === "#") break; if (ch === "*") relics += 1; } } return relics; }
