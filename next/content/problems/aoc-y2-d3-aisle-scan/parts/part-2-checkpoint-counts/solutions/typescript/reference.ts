export function scan_checkpoints(inputText: string): number { const prior: number[] = []; let exceeded = 0; for (const row of inputText.split("\n").filter(Boolean)) { let count = 0; for (const ch of row) { if (ch === "#") break; if (ch === "*") count += 1; } if (prior.length > 0 && count > prior[Math.floor((prior.length - 1) / 2)]) exceeded += 1; const pos = lowerBound(prior, count); prior.splice(pos, 0, count); } return exceeded; } function lowerBound(values: number[], target: number): number { let lo = 0, hi = values.length; while (lo < hi) { const mid = Math.floor((lo + hi) / 2); if (values[mid] < target) lo = mid + 1; else hi = mid; } return lo; }
