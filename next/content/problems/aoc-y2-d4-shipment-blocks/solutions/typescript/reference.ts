export function max_block_sum(inputText: string): number { let best = 0; for (const block of inputText.split("\n\n")) best = Math.max(best, blockSum(block)); return best; } function blockSum(block: string): number { let total = 0; for (const raw of block.split("\n")) { const line = raw.trim(); if (!line.includes("=")) continue; const value = line.split("=", 2)[1].trim(); if (/^\d+$/.test(value)) total += Number(value); } return total; }
