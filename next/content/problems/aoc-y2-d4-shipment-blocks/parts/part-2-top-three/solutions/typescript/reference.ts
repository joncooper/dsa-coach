export function top_three_block_sum(inputText: string): number { return inputText.split("\n\n").map(blockSum).sort((a, b) => b - a).slice(0, 3).reduce((a, b) => a + b, 0); } function blockSum(block: string): number { let total = 0; for (const raw of block.split("\n")) { const line = raw.trim(); if (!line.includes("=")) continue; const value = line.split("=", 2)[1].trim(); if (/^\d+$/.test(value)) total += Number(value); } return total; }
