export function elevation_triples(inputText: string): number { const lines = inputText.split("\n").filter((line) => line.trim()); if (lines.length === 0) return 0; const target = Number(lines[0]); const values = lines.slice(1).map(Number); let total = 0; for (let i = 0; i < values.length; i += 1) { const seen = new Map<number, number>(); for (let j = i + 1; j < values.length; j += 1) { const need = target - values[i] - values[j]; total += seen.get(need) ?? 0; seen.set(values[j], (seen.get(values[j]) ?? 0) + 1); } } return total; }
