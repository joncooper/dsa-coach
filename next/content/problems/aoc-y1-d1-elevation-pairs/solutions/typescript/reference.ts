export function elevation_pairs(inputText: string): number { const lines = inputText.split("\n").filter((line) => line.trim()); if (lines.length === 0) return 0; const target = Number(lines[0]); const counts = new Map<number, number>(); let pairs = 0; for (const line of lines.slice(1)) { const value = Number(line); pairs += counts.get(target - value) ?? 0; counts.set(value, (counts.get(value) ?? 0) + 1); } return pairs; }
