export function find_missing_stamp(inputText: string): number { const values = new Set<number>(); for (const raw of inputText.split("\n")) { const line = raw.trim(); if (line) values.add(parseInt(line, 36)); } const sorted = [...values].sort((a, b) => a - b); for (let i = 0; i < sorted.length - 1; i += 1) if (sorted[i + 1] - sorted[i] === 2) return sorted[i] + 1; return -1; }
