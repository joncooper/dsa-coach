export function max_stamp(inputText: string): number { let best = -1; for (const raw of inputText.split("\n")) { const line = raw.trim(); if (line) best = Math.max(best, parseInt(line, 36)); } return best; }
