# Authoring Guide

This repository supports MDX course authoring. The current bundled curriculum is generated from typed content data so validation can check every route, problem, quiz, and test case in one pass.

Future lessons can move from typed markdown strings into MDX files while keeping the same frontmatter schema.
